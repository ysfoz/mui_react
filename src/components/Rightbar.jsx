import { Box } from "@mui/material";

const Rightbar = () => {
  return (
    <Box bgcolor={"cyan"} flex={2} p={3} sx={{display:{xs:"none",sm:"block"}}}>
      Rightbar
    </Box>
  );
};

export default Rightbar;
